freedesktop/desktop.lua:

  - don't hardcode thunar; add a setting for that instead (an option is e.g. xdg-open)

*:

  - document functions and configuration options
  - include this code into awesome itself
